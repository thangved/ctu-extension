{
	"name": "ctu-management-system-extension",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"format": "prettier --write .",
		"prepare": "husky install",
		"dev": "esbuild scripts/index.ts --bundle --minify --sourcemap --outfile=build/bundle.js --watch"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"esbuild": "^0.18.3",
		"eslint": "^8.42.0",
		"husky": "^8.0.0",
		"prettier": "^2.8.8"
	},
	"dependencies": {
		"bootstrap": "5.3.0"
	}
}
